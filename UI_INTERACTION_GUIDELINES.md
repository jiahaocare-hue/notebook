# UI Interaction Guidelines

记录日期：2026-06-18

本文件用于统一后续按钮、动效、图标和反馈状态。当前应用仍在逐步收敛，新增 UI 应优先遵守这里的规则。

## 按钮

- 主操作：蓝色实心按钮，用于保存、搜索、生成、确认。
- 次操作：灰色或透明按钮，用于取消、浏览、刷新、返回。
- 危险操作：红色按钮，用于删除。
- 工具操作：优先使用图标或短文本，尺寸保持紧凑。
- 禁用状态：必须有 `disabled` 样式，避免 hover/scale 继续触发。

## 动效

- 常规按钮 hover 只做颜色或轻微阴影变化。
- 不在密集列表按钮上使用明显 scale，避免列表抖动。
- 加载状态使用小型 spinner，并用 `aria-busy` 标注。
- 模态和弹层动画保持短时长，避免影响重复操作。

## 反馈

- 保存成功、失败、需要重启必须明确展示。
- 后台任务失败不能只写 console，需要在相关 UI 位置展示原因。
- OCR、搜索、摘要生成等异步流程必须有 loading、success/error 或 empty 状态。

## 设置页信息架构

- 顶部显示配置总览：版本、数据目录状态、LLM 状态、OCR 日志数量。
- 常用设置放前面：主题、数据目录、日志。
- 高风险或长表单配置放后面：LLM、Prompt 模板、OCR 日志。
- 数据目录变更必须明确提示“重启后生效”。

